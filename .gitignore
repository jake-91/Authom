node_modules/
dist/
src-tauri/target/
*.local
.DS_Store
Thumbs.db

# Never commit a vault or anything derived from one. The live vault lives in
# AppData, but exports and hand-made copies land wherever the user saved them.
vault.json
vault.json.*
*.vault.json
authom-backup*.json
authom-plaintext*.txt

# Ad-hoc secrets that tend to appear during debugging
*.otpauth
secrets.txt

# Regenerated by Tauri on every build
src-tauri/gen/schemas/
